# HANDOFF

## 2026-08-17
- Androidのカメラ不良で横写真が縦のまま保存される問題に対応するため、回転機能を追加。
  - 実装：画像の実ピクセルをcanvasで再描画して回転（CSSのtransformではない）。
  - そのため書き出すJPEGにもそのまま反映される。
  - UI：写真編集エリアに「回転」項目を追加（左に90°／右に90°／180°）。
- Playwrightでブラウザ動作確認済み（回転後の寸法・ピクセル位置・console errorなし）。
- push済み、GitHub Pagesに反映済み。

## 2026-09-01
- 「Googleフォトを開いてダウンロード→ツールにアップロード」という手動フローが面倒という
  ことで、Googleフォトから直接写真を選び込める機能を追加（Google Photos Picker API +
  Google Identity Servicesを使用）。
  - 専用のGoogle Cloudプロジェクト（photo-layout-tool）を新規作成し、Photos Picker API
    を有効化、OAuthクライアントID（ウェブアプリケーション種別）を発行。
    - OAuth同意画面は「外部」「テスト中」ステータスで運用（審査未提出、テストユーザーに
      本人のGoogleアカウントを登録済み）。この状態だとリフレッシュトークンが短命なので、
      使うたびに再ログインを求められる可能性がある。
  - クライアントIDはコード内に直書き（公開前提の値のため問題なし）。クライアントシーク
    レットはこのフローでは不要のため一切使用していない（Google Cloud Console側に残した
    まま、リポジトリにもどこにも保存していない）。
  - フロー：ボタン押下→Googleログイン(ポップアップ)→Googleフォトのピッカー画面(別窓)で
    選択→ポーリングで完了検知→画像バイトを取得→写真一覧に追加。
  - ローカル環境ではOAuthのAuthorized Originの都合でテストできず、ボタン表示・既存の
    回転機能への影響が無いことのみ確認済み（実際のOAuth〜ピッカー〜取得フローは
    GitHub Pages上での実地テストが必要、本人に依頼中）。

## 次の一手
- Googleフォト連携の実地テスト待ち（うまく動くか、エラーが出るかの報告待ち）。
- それ以外は特になし。回転機能は完成済み。
- 今後Androidからカメラ運用をiPadに切り替える方向で検討中（未決定）。切り替われば
  このツールの回転機能は「過去の書き溜め写真の救済用」になる見込み。iPad + Safariで
  開けばOS標準の写真ピッカーが使えるため、Googleフォト連携自体の必要性も下がる可能性がある。
